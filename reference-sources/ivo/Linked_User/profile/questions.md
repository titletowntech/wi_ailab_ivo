# Questions — IVO Linked_User reference sample

Generated from the export. Every item needs a human answer before mapping.

## 1. `isActive`

Fill 100.0% · 2 distinct · observed integer

Sample values: `1` ×1052, `0` ×66

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 2. `isDeleted`

Fill 100.0% · 2 distinct · observed integer

Sample values: `0` ×1116, `1` ×2

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 3. `isMechanic`

Fill 100.0% · 2 distinct · observed integer

Sample values: `0` ×1098, `1` ×20

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 4. `isDriver`

Fill 100.0% · 2 distinct · observed integer

Sample values: `0` ×1117, `1` ×1

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.
