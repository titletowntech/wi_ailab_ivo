# Questions — IVO Company_Equipment_PM_Template reference sample

Generated from the export. Every item needs a human answer before mapping.

## 1. `intervalDays`

Fill 100.0% · 5 distinct · observed string

Sample values: `NULL` ×201, `365` ×11, `90` ×3, `1825` ×1, `0` ×1

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 2. `intervalOdometer`

Fill 100.0% · 9 distinct · observed string

Sample values: `NULL` ×195, `6000` ×7, `20000` ×5, `1000` ×2, `10000` ×2

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 3. `fixedHours`

Fill 100.0% · 9 distinct · observed string

Sample values: `NULL` ×206, `50` ×4, `1000` ×1, `100` ×1, `200` ×1

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 4. `triggerDays`

Fill 100.0% · 5 distinct · observed string

Sample values: `NULL` ×201, `7` ×9, `30` ×4, `5` ×2, `0` ×1

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 5. `triggerOdometer`

Fill 100.0% · 4 distinct · observed string

Sample values: `NULL` ×195, `200` ×15, `40` ×6, `30` ×1

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 6. `isDeleted`

Fill 100.0% · 2 distinct · observed integer

Sample values: `0` ×215, `1` ×2

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.
