# Questions — IVO WorkOrder reference sample

Generated from the export. Every item needs a human answer before mapping.

## 1. `hourMeterId`

Fill 100.0% · 731 distinct · observed string

Sample values: `NULL` ×993, `9901684` ×5, `9801861` ×2, `9801645` ×2, `9802128` ×2

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 2. `odometerId`

Fill 100.0% · 60 distinct · observed string

Sample values: `NULL` ×1671, `5201534` ×3, `5141217` ×2, `5141212` ×2, `5141366` ×2

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.

## 3. `isDeleted`

Fill 100.0% · 2 distinct · observed integer

Sample values: `0` ×1395, `1` ×342

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 4. `isActive`

Fill 100.0% · 2 distinct · observed integer

Sample values: `1` ×1395, `0` ×342

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 5. `isPmPerformed`

Fill 100.0% · 2 distinct · observed integer

Sample values: `1` ×963, `0` ×774

- **TYPE_MISMATCH** — Schema declares boolean; data looks like integer.

## 6. `substatusId`

Fill 100.0% · 48 distinct · observed string

Sample values: `NULL` ×1492, `70` ×75, `82` ×18, `74` ×13, `80` ×11

- **TYPE_MISMATCH** — Schema declares integer; data looks like string.
